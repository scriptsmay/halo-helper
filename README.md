# halo-helper

step 1. git clone [github_url]

setp 2.

```
cp app/config.example.yaml app/config.yaml
```

修改 `app/config.yaml` 为对应的配置。

step 3.

```
docker compose up -d
```

open the target url:

```
# random halo video
http://localhost:8003/api/v1/random-video
```
